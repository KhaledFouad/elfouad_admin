import 'package:flutter/material.dart';

class Trends extends StatelessWidget {
  const Trends({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: SizedBox(height: 220, child: Center(child: Text('LineChart (قريبًا)'))),
    );
  }
}